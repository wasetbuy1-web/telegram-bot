# مراجعة Commit 1 — لوحة التحكم (Dashboard)

> **الحالة:** لم يتم عمل commit أو branch. شجرة العمل تحتوي على 4 ملفات جديدة + سطرين مضافين في `app.ts`.
> **النطاق:** دالة `renderDashboard()` النقية فقط — لا تملك الأزرار أي وظيفة بعد (متوقّع في هذه المرحلة).
> **الفحص:** `npx tsc --noEmit` يمر بنجاح. جميع النصوص أدناه هي **مخرجات البرنامج الفعلية**، وليست رسومات تخيلية.

- **12** حالة
- فحص الأنواع: **نظيف**
- **عيبان** تم اكتشافهما أثناء المراجعة
- وضع التحليل: **HTML**
- الأزرار: **غير مفعّلة** في هذه المرحلة

---

## ⚠️ عيبان تم اكتشافهما أثناء هذه المراجعة

كلاهما في المُصيّر (renderer) فقط. لا يمنع أيٌّ منهما قرار التصميم — أخبرني أي إصلاح تريده وسأطبّقه قبل التجميد.

### العيب 1 — الشحن المباشر يُخفي تكلفته (الحالة G)

الجلسة تحتوي على `internationalShippingCost: 120`، لكن قسم الشحن يطبع فقط `الطريقة: مباشر`. رقم أدخله المستخدم يُحذف بصمت.

- **السبب:** دالة `shippingSection()` تقرأ المستودع والوزن وشركة الشحن، لكنها لا تقرأ حقل التكلفة المباشرة أبداً.
- **الإصلاح المقترح:** إظهار `تكلفة الشحن الدولي: 120$` عند اختيار الطريقة المباشرة، تماماً كما يظهر الوزن للشحن غير المباشر.
- **لماذا فات في المرة الأولى:** المعاينة الأولى جرّبت الشحن غير المباشر فقط. إضافة الحالة المباشرة لمجموعة الحالات هي ما كشفه.

### العيب 2 — عتبات الطيّ غير متوافقة (الحالتان I و J)

النص يُطوى عند 7 منتجات، لكن زر الإدارة يظهر فقط عند 9. لذا عند 7–8 منتجات تكون الرسالة مطويّة بينما تنمو لوحة المفاتيح إلى 10–11 صفاً — فتصبح الأزرار أطول من المحتوى الذي تتحكم فيه.

- **السبب:** `COMPACT_THRESHOLD = 6` يحكم النص، و`MANAGE_THRESHOLD = 8` يحكم الأزرار. قاعدتان لفكرة واحدة.
- **الإصلاح المقترح:** عتبة واحدة عند 6. بعدها يُطوى النص **و** تتحوّل أزرار الصفوف إلى `📦 إدارة المنتجات`، فتنطوي البطاقة كوحدة واحدة.
- **تكلفة الإصلاح:** تعديل المنتج السابع يحتاج شاشة إدارة بدل زر مباشر. تلك الشاشة من عمل Commit 2 على أي حال، ولوحة مفاتيح بـ11 صفاً هي المقايضة الأسوأ.

---

## الحالة A — فارغة

```
🧾 تسعيرة جديدة

لا توجد منتجات بعد

أرسل رابط المنتج مباشرة
أو اضغط ➕ إضافة منتج
```

**لوحة المفاتيح:**

| النص | الـ callback |
|------|--------------|
| ➕ إضافة منتج | `p:add` |
| 🏠 الرئيسية | `nav:home` |

**لماذا هذا التصميم.** لا عناوين أقسام، لا فواصل، لا كتلة شحن فارغة. التسعيرة الفارغة يجب أن تُقرأ كدعوة، لا كنموذج بفراغات.

- **العنوان يغيّر صياغته** — `تسعيرة جديدة` هنا، و`تسعيرة · مسودة` بمجرد وجود عمل، فتُقرأ الحالة من السطر الأول.
- **تلميح اللصق يسبق الزر** — أسرع مسار هو لصق رابط، لذا يُذكر قبل المسار الأبطأ.
- **لا زر إعدادات** — بحسب قرارك، تبقى اللوحة مركّزة.

---

## الحالة B — منتج واحد

```
🧾 تسعيرة · مسودة

━━━━━━━━━━━━━━━━━
📦 المنتجات (1)
━━━━━━━━━━━━━━━━━

① AirPods Pro
   249$ × 1
   amazon.com

القيمة: 249$

━━━━━━━━━━━━━━━━━
🚚 الشحن
━━━━━━━━━━━━━━━━━

لم يتم اختيار طريقة الشحن
```

**لوحة المفاتيح:**

| النص | الـ callback |
|------|--------------|
| ✏️ ① | `p:edit:<uuid>` |
| 🗑 ① | `p:del:<uuid>` |
| ➕ إضافة منتج | `p:add` |
| 🚚 مباشر | `sh:m:direct` |
| 📦 غير مباشر | `sh:m:indirect` |
| ❌ إلغاء التسعيرة | `q:cancel` |

**لماذا هذا التصميم.** يظهر قسم الشحن لحظة وجود منتج، لأن ذلك هو وقت اختيار الطريقة. تبقى شركات الشحن والإجماليات غائبة — لأنها ستكون ضجيجاً فقط.

- **ثلاثة أسطر لكل منتج** — الاسم، المبلغ، المصدر. يكفي للتعرّف عليه دون إعادة قراءة الرابط.
- **الأرقام داخل الدوائر** — `①②③` تربط كل صف بزرّه `✏️ ①`. الأزرار لا تُظهر أسماء طويلة، فالرقم هو المقبض المشترك.
- **سطر القيمة يتكرر عند منتج واحد** — متعمّد: موضع السطر لا يتغيّر عند إضافة منتجات.

---

## الحالة C — منتجان (الكمية > 1)

```
🧾 تسعيرة · مسودة

━━━━━━━━━━━━━━━━━
📦 المنتجات (2)
━━━━━━━━━━━━━━━━━

① AirPods Pro
   249$ × 1
   amazon.com

② Nike Air Max
   95$ × 2
   nike.com

القيمة: 439$

━━━━━━━━━━━━━━━━━
🚚 الشحن
━━━━━━━━━━━━━━━━━

لم يتم اختيار طريقة الشحن
```

**لوحة المفاتيح:** `✏️ ①` `🗑 ①` / `✏️ ②` `🗑 ②` / `➕ إضافة منتج` / `🚚 مباشر` `📦 غير مباشر` / `❌ إلغاء التسعيرة`

**لماذا هذا التصميم.** سطر القيمة يُظهر `439$` — أي 249 + (95 × 2). الكمية تُضرب في الإجمالي، لا تُعرض فقط، فيعني الرقم دائماً ما يفترضه القارئ.

- **سعر الوحدة يبقى سعر وحدة** — الصف يُظهر `95$ × 2`، وليس 190 مضروبة مسبقاً. أنت تعدّل أسعار الوحدة، فيجب أن تراها.
- **سطر فارغ بين المنتجات** — أنجع أداة لمسح قائمة على الهاتف.
- **التعديل والحذف في صف واحد** — لكل منتج، فيكون النمو صفاً واحداً لكل منتج بدل صفين.

---

## الحالة D — سعر ناقص + عملات مختلطة ⭐

```
🧾 تسعيرة · مسودة

━━━━━━━━━━━━━━━━━
📦 المنتجات (3)
━━━━━━━━━━━━━━━━━

① AirPods Pro
   249$ × 1
   amazon.com

② Adidas Samba
   95 EUR × 1
   zalando.de

③ Kindle
   ⚠️ السعر مطلوب
   ebay.com

القيمة: 249$ + 95 EUR

━━━━━━━━━━━━━━━━━
🚚 الشحن
━━━━━━━━━━━━━━━━━

لم يتم اختيار طريقة الشحن
```

**لوحة المفاتيح:** صفوف تعديل/حذف لكل منتج / `➕ إضافة منتج` / `🚚 مباشر` `📦 غير مباشر` / `❌ إلغاء التسعيرة`

**لماذا هذا التصميم.** هذه أهم حالة في المجموعة — حيث يُنتج المُصيّر الساذج رقماً خاطئاً بثقة.

- **العملات تُجمّع، لا تُضاف** — `249$ + 95 EUR`. جمع الدولار مع اليورو يعطي رقماً بلا معنى. التحويل مهمة محرّك التسعير، وهو غير مبني بعد، لذا يرفض العرض التخمين.
- **السعر الناقص تحذير على مستوى الصف** — `⚠️ السعر مطلوب` يجلس مكان السعر، فتظهر الفجوة بنظرة واحدة بجوار زر تعديلها.
- **المنتج الناقص يُحتسب** — العنوان يقول (3). إخفاؤه يجعل العدّاد يكذب.

---

## الحالة E — منتج بدون اسم

```
🧾 تسعيرة · مسودة

━━━━━━━━━━━━━━━━━
📦 المنتجات (1)
━━━━━━━━━━━━━━━━━

① بدون اسم
   249$ × 1
   amazon.com

القيمة: 249$

━━━━━━━━━━━━━━━━━
🚚 الشحن
━━━━━━━━━━━━━━━━━

لم يتم اختيار طريقة الشحن
```

**لماذا هذا التصميم.** يمكن أن يوجد منتج قبل أن يكون له اسم — بطاقة المنتج ستنشئه من رابط ملصوق. الصف يجب أن يبقى.

- **الصف لا ينهار أبداً** — `بدون اسم` يحفظ السطر فيبقى الترقيم والأزرار محاذَيَين للقائمة.
- **نائب مكان، لا تحذير** — بخلاف السعر الناقص، الاسم الناقص لا يمنع شيئاً. المشاكل الحقيقية فقط تحصل على `⚠️`، وإلا فقد العلامة معناها.

---

## الحالة F — شحن غير مباشر (محدّد بالكامل)

```
🧾 تسعيرة · مسودة

━━━━━━━━━━━━━━━━━
📦 المنتجات (1)
━━━━━━━━━━━━━━━━━

① AirPods Pro
   249$ × 2
   amazon.com

القيمة: 498$

━━━━━━━━━━━━━━━━━
🚚 الشحن
━━━━━━━━━━━━━━━━━

الطريقة: غير مباشر
المستودع: MyUS – Florida
وزن الشحنة: 4.7 lb
شركة الشحن: FedEx Economy
```

**لوحة المفاتيح:** `✏️ ①` `🗑 ①` / `➕ إضافة منتج` / `❌ إلغاء التسعيرة`

**لماذا هذا التصميم.** بمجرد اختيار الطريقة، تختفي أزرار الطريقة بدل بقائها كزوج محدّد. نص القسم يذكر الجواب أصلاً، فتكراره كأزرار يكون فائضاً.

- **تسمية-نقطتان-قيمة، سطر لكل عنصر** — الأعمدة المحاذاة بالمسافات تعيد ترتيب نفسها بشكل غير متوقّع في RTL عبر عملاء تليجرام. هذا الشكل يبقى في كل مكان.
- **الأسطر تظهر عند تعبئتها** — المستودع والوزن والشركة يظهر كلٌّ منها عند معرفته، فينمو القسم مع التقدّم الفعلي.
- **لا سعر بعد** — الشركة مسمّاة لكن غير مسعّرة. التسعير يأتي مع قائمة الشركات في Commit 3؛ إظهار سعر مخزّن قديم هو تحديداً العيب الذي يزيله نموذج الحالة الجديد.

---

## الحالة G — شحن مباشر ⚠️ (العيب 1)

```
🧾 تسعيرة · مسودة

━━━━━━━━━━━━━━━━━
📦 المنتجات (1)
━━━━━━━━━━━━━━━━━

① AirPods Pro
   249$ × 1
   amazon.com

القيمة: 249$

━━━━━━━━━━━━━━━━━
🚚 الشحن
━━━━━━━━━━━━━━━━━

الطريقة: مباشر
```

**العيب.** جلسة هذه الحالة تحتوي على `internationalShippingCost: 120`. القسم يُصيّر سطراً واحداً ويُسقطه. المستخدم الذي أدخل تكلفة شحن لا يرى دليلاً على تسجيلها.

- **السبب** — `shippingSection()` تقرأ المستودع والوزن والشركة، لكن لا تقرأ حقل التكلفة المباشرة أبداً.
- **الإصلاح المقترح** — إضافة `تكلفة الشحن الدولي: 120$` عند الطريقة المباشرة، بمحاكاة ظهور الوزن للشحن غير المباشر.

---

## الحالة H — 6 منتجات (آخر تصميم كامل)

```
━━━━━━━━━━━━━━━━━
📦 المنتجات (6)
━━━━━━━━━━━━━━━━━

① AirPods Pro
   100$ × 1
   shop0.com

② Nike Air Max
   125$ × 1
   shop1.com

③ Kindle Paperwhite
   150$ × 1
   shop2.com

④ Logitech MX Master
   175$ × 1
   shop3.com

⑤ Anker 737
   200$ × 1
   shop4.com

⑥ Sony WH-1000XM5
   225$ × 1
   shop5.com

القيمة: 975$
```

**لوحة المفاتيح:** 6 صفوف تعديل/حذف + `➕ إضافة منتج` + `🚚 مباشر` `📦 غير مباشر` + `❌ إلغاء التسعيرة` = 9 صفوف.

**لماذا هي الحدّ الفاصل.** ستة منتجات هي تقريباً حيث يتوقّف الصف ثلاثي الأسطر عن أن يستحق تكلفته — الرسالة تحتاج تمريراً ولوحة المفاتيح تسعة صفوف. (قسم الشحن حُذف من هذا المقتطف للطول.)

- **هذه آخر حالة كاملة التفاصيل** — عند السابع تنهار الصفوف إلى سطر واحد لكلٍّ.
- **تسعة صفوف مفاتيح مرتفعة أصلاً** — وهذا ما يجعل عدم التطابق في الحالتين التاليتين مشكلة حقيقية لا نظرية.

---

## الحالة I — 7 منتجات ⚠️ (العيب 2)

```
━━━━━━━━━━━━━━━━━
📦 المنتجات (7)
━━━━━━━━━━━━━━━━━

① AirPods Pro · 100$ × 1
② Nike Air Max · 125$ × 1
③ Kindle Paperwhite · 150$ × 1
④ Logitech MX Master · 175$ × 1
⑤ Anker 737 · 200$ × 1
⑥ Sony WH-1000XM5 · 225$ × 1
⑦ iPad Air · 250$ × 1

القيمة: 1,225$
```

**لوحة المفاتيح:** 7 صفوف تعديل/حذف + `➕` + `🚚`/`📦` + `❌` = **10 صفوف**.

**العيب.** الصفوف المطويّة تعمل — سبعة منتجات تُقرأ بوضوح في ثمانية أسطر. لكن لوحة المفاتيح لا تتبعها: عشرة صفوف تحكّم تحت ثمانية أسطر محتوى.

- **السبب** — `COMPACT_THRESHOLD = 6` يحكم النص، `MANAGE_THRESHOLD = 8` يحكم الأزرار. قاعدتان لفكرة واحدة.
- **الإصلاح المقترح** — عتبة واحدة عند 6. بعدها يُطوى النص **و** تصبح أزرار الصفوف `📦 إدارة المنتجات`، فتنطوي البطاقة كوحدة.
- **تكلفة الإصلاح** — تعديل المنتج 7 يحتاج شاشة إدارة بدل زر مباشر. تلك الشاشة عمل Commit 2 على أي حال، ولوحة بعشرة صفوف مقايضة أسوأ.

---

## الحالة J — 8 منتجات (الأسوأ) ⚠️

```
━━━━━━━━━━━━━━━━━
📦 المنتجات (8)
━━━━━━━━━━━━━━━━━

① AirPods Pro · 100$ × 1
② Nike Air Max · 125$ × 1
③ Kindle Paperwhite · 150$ × 1
④ Logitech MX Master · 175$ × 1
⑤ Anker 737 · 200$ × 1
⑥ Sony WH-1000XM5 · 225$ × 1
⑦ iPad Air · 250$ × 1
⑧ Apple Watch SE · 275$ × 1

القيمة: 1,500$
```

**لوحة المفاتيح:** 8 صفوف تعديل/حذف + `➕` + `🚚`/`📦` + `❌` = **11 صفاً**.

**عدم التطابق في أسوأ صوره.** أحد عشر صف مفاتيح تحت تسعة أسطر نص. على الهاتف تملأ الأزرار الشاشة وتُدفع التسعيرة نفسها خارج المشهد — عكس لوحة التحكم تماماً.

- **نفس السبب الوحيد للحالة I** — إصلاح العتبة يزيل كليهما.

---

## الحالة K — 9 منتجات (مطويّة، كما هو مقصود)

```
━━━━━━━━━━━━━━━━━
📦 المنتجات (9)
━━━━━━━━━━━━━━━━━

① AirPods Pro · 100$ × 1
② Nike Air Max · 125$ × 1
③ Kindle Paperwhite · 150$ × 1
④ Logitech MX Master · 175$ × 1
⑤ Anker 737 · 200$ × 1
⑥ Sony WH-1000XM5 · 225$ × 1
⑦ iPad Air · 250$ × 1
⑧ Apple Watch SE · 275$ × 1
⑨ Magic Keyboard · 300$ × 1

القيمة: 1,800$
```

**لوحة المفاتيح:**

| النص | الـ callback |
|------|--------------|
| 📦 إدارة المنتجات (9) | `p:manage` |
| ➕ إضافة منتج | `p:add` |
| 🚚 مباشر / 📦 غير مباشر | `sh:m:direct` / `sh:m:indirect` |
| ❌ إلغاء التسعيرة | `q:cancel` |

**لماذا هذا التصميم ينجح.** هذا ما يجب أن تبدو عليه الحالتان I و J. تسعة منتجات تبقى مقروءة بالكامل في عشرة أسطر، ولوحة المفاتيح تعود إلى أربعة صفوف — نفس ارتفاع تسعيرة بمنتجين.

- **المحتوى يتوسّع، الإطار لا** — القائمة تنمو؛ الأزرار ثابتة. هذه الخاصية تجعلها تُحَسّ كلوحة تحكم لا كنموذج متضخّم.
- **العدد يبقى في الزر** — `إدارة المنتجات (9)` يُبقي الرقم مرئياً بعد اختفاء أزرار الصفوف.
- **الأرقام داخل الدوائر تغطّي 1–10** — بعدها يرجع المُصيّر لأسلوب `.11`. جدير بالمعرفة قبل بناء تسعيرة من 12 عنصراً.

---

## الحالة L — اسم غير آمن (العطل الذي لم يحدث)

```
🧾 تسعيرة · مسودة

━━━━━━━━━━━━━━━━━
📦 المنتجات (1)
━━━━━━━━━━━━━━━━━

① Nike Air_Max <Special> & *Bold*
   199$ × 1
   x.com

القيمة: 199$
```

**ما يُرسل فعلياً إلى تليجرام (صيغة السلك):**

```
① Nike Air_Max &lt;Special&gt; &amp; *Bold*
   199$ × 1
   x.com
```

**لماذا توجد هذه الحالة.** منتج اسمه `Nike Air_Max <Special> & *Bold*` هو تحديداً المُدخل الذي يجعل شاشات Markdown الحالية تُطلق خطأ تحليل وتفشل في التصيير تماماً. هنا يُصيَّر كما كُتب.

- **صيغة السلك مقابل العرض** — البوت يرسل `&lt;Special&gt;`؛ تليجرام يُظهر `<Special>`. الهروب (escaping) هو سبب بقاء الرسالة.
- **النجوم تبقى حرفية** — تحت وضع HTML يكون `*Bold*` نصاً لا تنسيقاً. تحت Markdown كان سيصبح خطأ تحليل.
- **كل قيمة مستخدم تُهرَّب** — أسماء المنتجات، المواقع، أسماء المستودعات، أسماء الشركات. لا شيء يصل السلك بلا هروب.

---

## الخلاصة

**لا شيء مُلتزَم (committed).** لا branch، لا commit — شجرة العمل تحتوي على 4 ملفات جديدة وسطرين مضافين في `app.ts`.

قل أي الإصلاحين تريد — كليهما، أحدهما، أو لا شيء — وسأطبّقه، أعيد التصيير لنظرة أخيرة، ثم أجمّد اللوحة وأبدأ بطاقة المنتج.

| | القيمة |
|---|---|
| الملفات الجديدة | `src/dashboard/dashboard.ts` · `src/utils/html.ts` · `src/constants/callbacks.ts` · `src/handlers/dashboard.handler.ts` |
| المعدّلة | `src/app.ts` (سطران) |
| فحص الأنواع | يمر ✅ |
| العيوب | 2 (كلاهما في المُصيّر، إصلاح بسيط) |
